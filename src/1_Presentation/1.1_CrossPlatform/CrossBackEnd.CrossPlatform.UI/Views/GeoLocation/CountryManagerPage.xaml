<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    x:Class="CrossBackEnd.CrossPlatform.UI.Views.GeoLocation.CountryManagerPage"
    
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Country Manager"
    >

    <StackLayout Orientation="Vertical">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <SearchBar x:Name="txtSearch" Grid.Column="0" SearchCommand="{Binding SearchCommand}"/>
            <Button Grid.Column="1" Text="+" BackgroundColor="LightGreen"/>
        </Grid>
        <ListView/>
    </StackLayout>
    
    <!--
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Text="Pais:" Grid.Row="1" Grid.Column="1" Style="{StaticResource StyleBaseLabel}"/>
        <Entry Placeholder="Digite o Nome do Pais" Grid.Row="1" Grid.Column="2" Style="{StaticResource StyleBaseTextBox}"/>
        <Label Text="Abreviatura:" Grid.Row="2" Grid.Column="1" Style="{StaticResource StyleBaseLabel}"/>
        <Entry Placeholder="Digite a Abreviatura" Grid.Row="2" Grid.Column="2" Style="{StaticResource StyleBaseTextBox}"/>
        <StackLayout Orientation="Horizontal" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" HorizontalOptions="Center" Margin="0,20,0,0">
            <Button Text="Save" Grid.Row="4" Grid.Column="1" BackgroundColor="Green" TextColor="White"/>
            <Button Text="Cancel" Grid.Row="4" Grid.Column="2" BackgroundColor="LightGray" TextColor="White"/>
        </StackLayout>
        
    </Grid>
    -->
</ContentPage>